import { IconUtil } from "./_utils/iconUtil";

const side = 512;
const scale = 1.0;

export const contentType = "image/png";

export const alt = "Route Drop";

export const size = {
  width: side,
  height: side,
};

const Icon = IconUtil({ side, scale });

export default Icon;
